#
# Executes commands at the start of an interactive session.
#
# Authors:
#   Sorin Ionescu <sorin.ionescu@gmail.com>
#

# Source Prezto.
if [[ -s "${ZDOTDIR:-$HOME}/.zprezto/init.zsh" ]]; then
  source "${ZDOTDIR:-$HOME}/.zprezto/init.zsh"
fi

# Customize to your needs...
export LC_CTYPE="en_US.UTF-8"
export LC_COLLATE="C"
export EDITOR=vim
export VISUAL=vim
export ANDROID_HOME=/usr/local/opt/android-sdk
if [ -f ~/.homebrew_gh_api_token ]; then
	export HOMEBREW_GITHUB_API_TOKEN=$(cat ~/.homebrew_gh_api_token)
fi

# aliases
alias ff='find ./ -iname $*'
alias vi='vim'
alias ssh-kr='ssh-keygen -R $*'

# I don't like questions
unalias rm
unalias cp
unalias mv

# Avoid this boring message "do you wish to see all N possibilities?
export LISTMAX=1000

# Disable correct completely, it's PITA
unsetopt correct correctall

# Add ~/bin to PATH
PATH=${PATH}:~/bin

# macOS specific bits
if [ $(uname -s) = "Darwin" ]; then
	if which -s brew >/dev/null 2>&1; then
		PATH="$(brew --prefix homebrew/php/php56)/bin:$PATH"
	fi
	export XGETTEXT=/usr/local/Cellar/gettext/0.19.8.1/bin/xgettext
	export MSGFMT=/usr/local/Cellar/gettext/0.19.8.1/bin/msgfmt
	export GOPATH=/usr/local/opt/go/libexec
	PATH=$PATH:/usr/local/opt/go/libexec/bin
fi

export PATH

# alt+. insert last word from previous command
bindkey -M viins '\e.' insert-last-word

setopt clobber

# Fix git reset HEAD^
unsetopt nomatch

# keychain
if which -s keychain >/dev/null 2>&1 && test -f ~/.ssh/id_rsa; then
	eval $(keychain --eval id_rsa)
fi

# iTerm2 integration
test -e ${HOME}/.iterm2_shell_integration.zsh && source ${HOME}/.iterm2_shell_integration.zsh

# Disable noglob scp
unalias scp
